export interface Video {
  id: string;
  title: string;
  thumbnail: string;
  channel: string;
  channelId: string;
  views: string;
  duration: string;
  uploadedAt: string;
  description?: string;
  youtubeId?: string;
}